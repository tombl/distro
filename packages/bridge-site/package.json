{
  "name": "@lowland/bridge-site",
  "private": true,
  "type": "module",
  "exports": {
    "./client": {
      "types": "./client.d.ts",
      "default": "./client.js"
    }
  },
  "scripts": {
    "check": "playwright test --list",
    "pretest": "pnpm --dir ../.. artifacts && pnpm --filter=@lowland/bytes build && pnpm --filter=@lowland/kernel build && pnpm --filter=@lowland/guest build",
    "test": "playwright test"
  },
  "dependencies": {
    "@lowland/bytes": "workspace:*",
    "@lowland/guest": "workspace:*",
    "@lowland/kernel": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  }
}
