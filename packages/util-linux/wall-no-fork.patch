wall forks only after a nonblocking tty write would block, so a slow terminal
cannot hold up delivery to the others.  Keep normal delivery and report that
individual terminal as timed out on the no-fork target.

--- a/term-utils/ttymsg.c
+++ b/term-utils/ttymsg.c
@@ -137,6 +137,14 @@ char *ttymsg(struct iovec *iov, size_t iovcnt, const char *line, int tmout)
 			continue;
 		}
 		if (errno == EWOULDBLOCK) {
+#ifdef __wasm__
+			len = snprintf(errbuf, sizeof(errbuf),
+				       _("%s: write would block"), device);
+			if (len < 0 || (size_t) len >= sizeof(errbuf))
+				snprintf(errbuf, sizeof(errbuf), _("write timed out"));
+			close(fd);
+			return errbuf;
+#else
 			int cpid, flags;
 			int rc;
 			sigset_t sigmask;
@@ -175,6 +183,7 @@ char *ttymsg(struct iovec *iov, size_t iovcnt, const char *line, int tmout)
 				_exit(EXIT_FAILURE);
 			}
 			continue;
+#endif
 		}
 		/*
 		 * We get ENODEV on a slip line if we're running as root,
