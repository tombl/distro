--- a/include/timer.h
+++ b/include/timer.h
@@ -20,7 +20,7 @@ struct ul_timer {
 #endif
 
 extern int setup_timer(struct ul_timer *timer, struct itimerval *timeout,
-		       void (*timeout_handler)(int, siginfo_t *, void *));
+		       void (*timeout_handler)(int));
 extern void cancel_timer(struct ul_timer *timer);
 
 #endif /* UTIL_LINUX_TIMER_H */
--- a/lib/timer.c
+++ b/lib/timer.c
@@ -33,7 +33,7 @@
 #ifdef HAVE_TIMER_CREATE
 int setup_timer(struct ul_timer *timer,
 		struct itimerval *timeout,
-		void (*timeout_handler)(int, siginfo_t *, void *))
+		void (*timeout_handler)(int))
 {
 	time_t sec = timeout->it_value.tv_sec;
 	long usec = timeout->it_value.tv_usec;
@@ -52,8 +52,8 @@ int setup_timer(struct ul_timer *timer,
 	if (sigemptyset(&sig_a.sa_mask))
 		return 1;
 
-	sig_a.sa_flags = SA_SIGINFO;
-	sig_a.sa_sigaction = timeout_handler;
+	sig_a.sa_flags = 0;
+	sig_a.sa_handler = timeout_handler;
 
 	if (sigaction(SIGALRM, &sig_a, NULL))
 		return 1;
@@ -72,14 +72,14 @@ void cancel_timer(struct ul_timer *timer)
 
 int setup_timer(struct ul_timer *timer,
 		struct itimerval *timeout,
-		void (*timeout_handler)(int, siginfo_t *, void *))
+		void (*timeout_handler)(int))
 {
 	struct sigaction sa;
 
 	memset(&sa, 0, sizeof sa);
 	memset(timer, 0, sizeof(*timer));
 
-	sa.sa_flags = SA_SIGINFO | SA_RESETHAND;
-	sa.sa_sigaction = timeout_handler;
+	sa.sa_flags = SA_RESETHAND;
+	sa.sa_handler = timeout_handler;
 
 	if (sigaction(SIGALRM, &sa, &timer->old_sa))
 		return 1;
--- a/sys-utils/flock.c
+++ b/sys-utils/flock.c
@@ -95,14 +95,9 @@ static void __attribute__((__noreturn__)) usage(void)
 
 static volatile sig_atomic_t timeout_expired = 0;
 
-static void timeout_handler(int sig __attribute__((__unused__)),
-			    siginfo_t *info,
-			    void *context __attribute__((__unused__)))
+static void timeout_handler(int sig __attribute__((__unused__)))
 {
-#ifdef HAVE_TIMER_CREATE
-	if (info->si_code == SI_TIMER)
-#endif
-		timeout_expired = 1;
+	timeout_expired = 1;
 }
 
 static int open_file(const char *filename, int *flags)
@@ -376,6 +371,13 @@ int main(int argc, char *argv[])
 	if (verbose)
 		gettime_monotonic(&time_start);
 	while (do_lock(api, fd, type, block, start, length)) {
+		/*
+		 * wasm signal delivery can interrupt the blocking lock without
+		 * preserving EINTR in errno. The handler flag is authoritative.
+		 */
+		if (timeout_expired)
+			exit(conflict_exit_code);
+
 		switch (errno) {
 		case EWOULDBLOCK:
 			/*
