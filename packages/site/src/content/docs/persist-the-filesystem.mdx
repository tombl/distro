---
title: Persist the filesystem
description: Keep a guest's writable data across page loads.
section: howtos
order: 6
---

# Persist the filesystem

This page has not been written yet. It will cover storing a guest's writable data in OPFS so it survives page reloads,
and what the packaged read-only root image does and does not let you
persist.
