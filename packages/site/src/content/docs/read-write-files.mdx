---
title: Read and write files
description: Move files and data between the host and a guest with guest.fs.
section: howtos
order: 2
---

# Read and write files

This page has not been written yet. It will cover the `guest.fs` API for text and binary files, directories and
metadata, which paths are writable, and moving larger data in and out
of a guest efficiently.
