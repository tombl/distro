---
title: Run a process
description: Run commands in a guest and work with their input, output, and exit codes.
section: howtos
order: 1
---

# Run a process

This page has not been written yet. It will cover running commands with `guest.exec`, why it takes an argv array instead
of a command string, writing to stdin, streaming stdout and stderr
incrementally, exit codes, and setting environment variables and a
working directory.
