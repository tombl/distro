{
  "name": "@tombl/linux-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "astro build",
    "dev": "astro dev",
    "check": "tsc",
    "setup": "rm -f public/rootfs.ext4.gz public/initramfs.cpio.gz && nix build .#rootfs -o result-rootfs && gzip --best --no-name --stdout result-rootfs > public/rootfs.ext4.gz && nix build .#initramfs -o result-initramfs && gzip --best --no-name --stdout result-initramfs > public/initramfs.cpio.gz"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/addon-webgl": "^0.18.0",
    "@xterm/xterm": "^5.5.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@astrojs/mdx": "^7.0.3",
    "@deno/doc": "jsr:0.202.0",
    "@types/node": "^26.1.1",
    "astro": "^7.1.1",
    "typescript": "^7.0.2"
  }
}
